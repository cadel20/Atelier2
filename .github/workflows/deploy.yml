name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Verify public directory
      run: |
        echo "🔍 Vérification du dossier public/"
        
        if [ ! -d "public" ]; then
          echo "❌ ERREUR: Le dossier 'public/' est requis pour GitHub Pages"
          echo "   Créez un dossier 'public/' avec:"
          echo "   - public/index.html"
          echo "   - public/css/style.css (optionnel)"
          echo "   - public/js/ (optionnel)"
          exit 1
        fi
        
        if [ ! -f "public/index.html" ]; then
          echo "⚠️  ATTENTION: public/index.html manquant"
          echo '<h1>DevOps Pipeline</h1>' > public/index.html
          echo "✅ index.html créé"
        fi
        
        echo "✅ Dossier public/ prêt"
        echo "📊 Contenu:"
        find public -type f | sed 's/^/  📄 /' 2>/dev/null | head -15
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: "public"
    
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Show result
      run: |
        echo "✅ SITE DÉPLOYÉ !"
        echo "🌐 ${{ steps.deployment.outputs.page_url }}"
        echo "📁 Votre CSS est disponible à:"
        echo "   ${{ steps.deployment.outputs.page_url }}css/style.css"